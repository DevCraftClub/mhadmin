{% macro double_slider(id, label, descr, value, opts) %}
    {% include 'core/includes/form/templateIncludes/field_row.twig' with {
        widget: 'core/includes/form/templateIncludes/double_slider.twig',
        id: id,
        label: label,
        descr: descr,
        value: value,
        opts: opts|default({})
    } %}
{% endmacro %}
