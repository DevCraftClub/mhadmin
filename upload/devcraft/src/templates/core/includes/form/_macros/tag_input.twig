{% macro tag_input(id, label, descr, value, opts) %}
    {% include 'core/includes/form/templateIncludes/field_row.twig' with {
        widget: 'core/includes/form/templateIncludes/tag_input.twig',
        id: id,
        label: label,
        descr: descr,
        value: value,
        opts: opts|default({})
    } %}
{% endmacro %}
