{# Shown on dashboard when legacy maharder directory still exists (T048). #}
<div class="devcraft-legacy-notice" style="margin:1rem 0;padding:1rem;background:#fff8e1;border:1px solid #ffc107;border-radius:4px;">
    <strong>Migration:</strong> Legacy-Verzeichnis <code>engine/inc/maharder/</code> ist noch vorhanden.
    Nach erfolgreicher Prüfung können Sie es manuell entfernen.
</div>
