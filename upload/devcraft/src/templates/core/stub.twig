<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    <style>
        body { font-family: system-ui, sans-serif; margin: 2rem; }
        .devcraft-stub { max-width: 40rem; padding: 1.5rem; border: 1px solid #ddd; border-radius: 8px; }
        h1 { margin-top: 0; color: #1a1a2e; }
        .version { color: #666; font-size: 0.9rem; }
    </style>
</head>
<body>
    <div class="devcraft-stub">
        <h1>{{ title }}</h1>
        <p class="version">Version {{ version }}</p>
        <p>{{ message }}</p>
    </div>
</body>
</html>
